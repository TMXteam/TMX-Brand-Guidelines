v2.4.2  2026-07-21  — tokens.css and the page :root now carry the full four-tier radius scale: added --r-panel (16px, for panels, drawers, images) and --r-field (12px, for fields, inline callouts) beside the existing --r-card (22px) and --r-pill (9999px). This matches the radius canon the live site formalized (globals.css --radius-* / DESIGN.md §8.5–8.6), where the old 18px folded into 16 and 10px into 12, leaving four named tiers. The reproduction set had shipped only card and pill since the v2.0.0 slim; a partner or app reproducing Predixa's inputs and panels now pulls those two radii from the kit instead of eyeballing them. No colour, type, spec, or existing value changed.
v2.4.1  2026-07-17  — §03 "the four assets": reordered both the tiles and the usage table to flat, 3D, mono, globe, and leveled the 3D preview so it no longer out-sizes the row. The 3D render is the only portrait export and bleeds to its frame edges, so under the shared width constraint it rendered tallest at every breakpoint; it now sits in a square-ish box (aspect-ratio) that lands level with the other marks. Preview display only; the shipped 3D asset is unchanged.
v2.4.0  2026-07-17  — Renamed the kit from "Partner Design-Spec Kit" to "Brand Kit" across the page chrome: the <title>, the house-strip tag, the cover kind label, the eyebrow, and the footer. No spec, asset, or value changed.
v2.3.1  2026-07-17  — Rebuilt the download-everything pack (TMX-predixa-assets.zip): it had frozen at a pre-v2.3.0 build, still shipping the four byte-identical Inter/JetBrains-Mono webfonts dropped in v2.3.0 and omitting the three static Fraunces install cuts added in v2.1.0, so it contradicted fonts/ and the manifest. predixa-logo-mono.svg had exported without a fill and rendered pure #000; it now carries brand ink #0A0A0C, matching the ecosystem mono mark and the never-#000 rule. The page's internal --accent-deep custom property is renamed --accent-text, matching the token tokens.css has shipped since v2.0.0. manifest.md now lists the assets and essentials zips beside the fonts zip. No brand value changed.
v2.3.0  2026-07-17  — §05 rebuilt as field-matted swatch cards, matching the ambassadors hub. Every value is now its own copy button carrying the ⧉ glyph at rest, so a hex, RGB, foil string or gradient stop reads as copyable without being hovered first; each foil splits into per-stop eyedropper dots, each copying its own hex. Gold no longer touches paper: in light mode the accent and foil swatches rendered #FFB627 straight onto the warm-paper field, contradicting this kit's own "Gold never touches paper" hard rule in the same section and reproducing the image §09 files under "Don't", while a swatch captioned "11.3:1 on ink (AAA)" sat on a field that wasn't ink. Accents and foils are now matted on the field they are tuned for, and the mat's hairline keys to the ground, not the page, so the light/dark indicator keeps an edge when the page theme matches it. Shared neutrals keep no theme split and stay full-bleed, with an ink-keyed inset hairline so whichever neutral matches the page (#0A0A0C in dark, #F6F2E8 in light) still draws its own edge. Fixes: swatch fills printed as empty outlines because print-color-adjust:exact named .sw .chip and not the stop marks (now hung on .sw, which inherits); a blocked clipboard failed silently, so a partner who read the missing ✓ as a dropped animation would paste a stale value and ship it as brand colour (now says so and selects the value for manual copy); 28 aria-live regions each frozen by their own aria-label announced nothing (now one shared status region); copy targets measured 18.6px against WCAG 2.2 SC 2.5.8's 24px. Inter's 400/500/600/700 files were four byte-identical copies of one variable font, downloaded separately: collapsed to a single face over wght 100-900, and the four redundant binaries dropped from fonts/ and the manifest. §03 drops the two on-ink/on-paper plates and the iOS app-icon mockup, and merges the Flat/3D pair and the lockup row into one "three assets" row (Flat / 3D / Globe emblem): the flat vector was rendering three times in the section (both plates, then again as the Flat tile) and "Primary · shield" was the same file as "Flat", while at 52px the flat and 3D finishes were indistinguishable and the globe emblem was too small to read. Tiles now scale to 196px. The orphaned .plate / .logo-feature / .app-icon rules are removed with them. Any hex quoted in prose (§03's ink/paper placements, §05's law) is a copy button too. The matted ground drops its inset hairline: it drew a line inside the mat rather than an edge around it, and when a ground matches the page theme it should merge into the card, because the card is that field. The §03 facets block is dropped: the shield's gold and green sides are already stated in the section's own opening sentence, and §05 owns both colours. Each asset in §03 now downloads on its own: the flat mark as its SVG vector master, the 3D render as its full-size PNG, and the globe emblem as both the dark and the light PNG, since it is the one asset that ships a per-theme pair. The section's zips stay for whoever wants everything. §03 shows four assets, not three: the mono mark was shipped in the zip and listed in §10 but never shown. It joins the row as a per-theme pair, the same shape as the globe emblem (reversed white on ink, black silhouette on paper), carrying its vector plus both PNG cuts. The row is 2x2 rather than auto-fit, which landed 3+1 and orphaned the fourth tile at the width the body column actually gets. No brand value changed.
v2.2.0  2026-07-17  — Rewritten in a reference register (2104 → 1561 words, 26%); every hex, ratio, rule and asset retained. The kit had inherited the umbrella site's editorial voice, but a partner kit is scanned, not read: prose that carried no spec value is gone ("the same duality that runs through the product", "the typewriter punctuation that frames each section", "The real payload of this kit is…", "The laws above, shown rather than asserted", the SIL OFL sentence whose compliance the shipped license files already carry), and the surviving prose, table cells and list items are tightened to the same register. Design rationale aimed at the brand team rather than the partner ("a deliberate decision, not a mechanical inversion") dropped; the §08 anti-impersonation rationale is deliberately kept, since it is what makes partners comply rather than improvise. Preceding redundancy pass, no spec value removed: every deleted sentence had a surviving twin elsewhere in the kit. The cover claimed "the one hard rule" while the body shipped five badges; the cover now reads "the rule that matters most" and the badges stand. §00 absorbs §01's "what you're reproducing" clause, leaving §01 as the hard-rule legend plus policy/contact. §03 states flat-vs-3D once instead of four times, and drops the Do/Don't bullets that restated it and the §08 ad law. §05 drops the closing paragraph (each clause survives above: "mechanical inversion" in the lead, never-#000/#FFF on the neutral swatches) and the two daylight swatches' repeated role lists. §06's install callout drops the optical-size repeat, stated once in the type rules and once in the zip's README. §07 drops the fine print restating the paragraph above it, keeping the minimum-size clause. §08 drops the lead that pre-announced its own law. §09 drops the sentence explaining what a Do/Don't gallery is. §10 drops the prose enumeration of the file table beneath it and the second "grouped into logo/, globe/, favicon/, fonts/" 20 lines after the first. Unlike the ecosystem kit, §02's lane law and §03's facets block are retained: Predixa's §05 law does not subsume the lane rule, and the Predixa shield genuinely has two facets.
v2.1.0  2026-07-17  — installable fonts: fonts/install/ desktop TTFs (Inter, Fraunces, JetBrains Mono) with OFL licenses and a TMX-predixa-fonts.zip download in §06. Fraunces ships as four cuts: the default 14pt Medium Italic (outline-identical to the font the live site serves, verified glyph-by-glyph), the stylised 72pt and 144pt display cuts, and the variable italic on an opsz 9–144 slider defaulting to 14. Fraunces has no static Medium upstream (Thin/Light/Regular/SemiBold/Bold/Black only), so all four are instanced from the official variable font at weight 500; Fraunces declares no Reserved Font Name, so they keep the family name. The install variable previously defaulted to Black at opsz 9 — installing it and picking "Fraunces" gave the wrong weight and the wrong optical size — and now defaults to Medium at 14. The bundled webfont is cut static at opsz 14 rather than carrying the live 9–144 axis: with the axis live, the browser's default font-optical-sizing:auto rendered the kit's display specimens near opsz 60–90 while the site rendered 14, so kit and site disagreed. Freezing it removes the drift and drops the file 42.4kB → 22.6kB.
v2.0.0  2026-07-16  — Marketing-first trim: §01 collapsed to a short note that defers to the usage & trademark policy; §02's loudness band collapsed to a three-row house strip whose accent chips follow the theme (daylight counterparts on paper, incl. print); §05 swatches cut to the accent pair per theme plus neutrals, with the foil shown as one copyable gradient per theme; §06 drops the type-scale table; §08's ad spec cut to three bullets (the CTA pill is now stated per field: gold on ink, emerald on paper); §09 light/dark laws folded into §05 as one line; tokens.css slimmed to the reproduction set (drops surface-solid/select/yes/no/divider/pad, folds mid/high into --foil, renames accent-deep to --accent-text); tokens.json removed; Do/Don't renumbered §09, Assets §10; swatches list hex and RGB, each copyable on its own line (replaces the chip copy band); the cover gains two downloads: the full pack and a four-mark essentials zip (flat vector, 3D render, both globe masters); review pass: the §06 foil specimen now actually renders the foil (the cascade painted it ink), light-field Do/Don't status text derives an AA ink-mix, print pins the foil/badge/pill fills, the closed mobile drawer leaves the tab order, and scroll regions plus copy feedback are keyboard- and screen-reader-accessible; removed §07's unintroduced "Predixa secondary" variant name.
v1.7.0  2026-07-16  — §03 now introduces the four-leaf clover that has always been part of the shield, and §07's no-AI rule names it among the things a redraw gets wrong; corrected the §09 light-mode candle fills to #0F7D43 / #C0392B (the light column repeated the dark values); the hero mark's shadow now warms on paper instead of dropping black; added a skip link ahead of the section list; §02 zones captioned as fixed identity swatches; §03 notes the 3D render is one asset for both themes.
v1.6.5  2026-07-16  — Do/Don't tiles and the on-ink/on-paper plates now pin the field they depict, so a specimen no longer borrows the page theme's palette (the gold foil stayed emerald on the ink card in light mode, and the paper tiles' Do/Don't badges were unreadable in dark mode); the light-theme top bar drops backdrop-blur for an opaque paper bar, per the kit's own light/dark law; printing now carries the full light token set instead of only background and ink, and specimen fields survive the printer's background stripping.
v1.6.4  2026-07-14  — Reverted the §02 "you are here" zone (v1.6.2–v1.6.3): back to the original theme-driven outline (light in dark mode, dark in light mode).
v1.6.2  2026-07-14  — §02 "Which TMX is this" now follows the theme (the Predixa zone is emerald on the light field, not stuck on gold, per the gold-by-night / emerald-by-day rule); click-to-copy swatch band now fills the chip's full top edge (removed the 1px inset gap).
v1.6.1  2026-07-14  — Hero mark now renders in 3D; equalized the §03 on-ink/on-paper plates so both marks align (facets moved to a row below); vertically centered the three-lockup tiles.
v1.6.0  2026-07-14  — Refreshed the 3D shield render with deeper relief (updated the 3D PNGs and the download pack) and added a §03 note that the finish only reads at large, premium sizes—ship the flat vector below that.
v1.5.0  2026-07-14  Navigation redesign across breakpoints (safe-area/notch fix, click-to-close scrim), sun/moon theme toggle icon, click-to-copy colour swatches.
v1.4.0  2026-07-14  — Light is now the default theme (dark stays the canonical reference); campaigns lead in light; replaced the mislabeled "app glyph" favicon lockup with an honest favicon entry plus an illustrative iOS app-icon mockup (squircle tile).
v1.3.0  2026-07-14  Added a §03 flat-vs-3D finish showcase and renamed the download to TMX-predixa-assets.zip.
v1.2.1  2026-07-14  — Neutral kit-purpose framing (dropped co-branded assumption), removed the TMX Global disclaimer sentence, globe emblem now switches dark/light with theme.
v1.2.0  2026-07-12  Added WCAG contrast ratios to colour swatches, in-kit contact/approval line, machine-readable token export (tokens.css/json).
v1.1.0  2026-07-12  11px type-floor bump; fixed §09 status-fill contradiction, light-foil swatch mismatch, removed unintroduced "shamrock", added §03 globe dark/light note, monotonic foil gradient (light to dark), copy tightening.
v1.0.0  2026-07-12  Initial Predixa partner kit.
