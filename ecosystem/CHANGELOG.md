v2.1.0  2026-07-17  — installable fonts: fonts/install/ desktop variable TTFs (Inter, Fraunces Italic, JetBrains Mono) with OFL licenses, TMX-ecosystem-fonts.zip download in §06, Fraunces pinned to optical size 72 as canon
v2.0.0  2026-07-16  — Marketing-first trim: §01 collapsed to a short note that defers to the usage & trademark policy; §02's loudness band collapsed to a three-row house strip whose accent chips follow the theme (daylight counterparts on paper, incl. print); §05 swatches cut to one accent per theme plus neutrals, with the foil shown as one copyable gradient per theme (drops the mislabeled #8B3F1F and the near-duplicate Deep Terracotta; the accent itself is the small-text value on both fields, matching the live site); §06 drops the type-scale table; §08's ad spec cut to three bullets; §09 light/dark laws folded into §05 as one line (removing the stale constant-candle-fill claim); tokens.css slimmed to the reproduction set (drops surface-solid/select/yes/no/divider/pad and the accent deep/mid/high trio; the foil carries its stops); tokens.json removed; Do/Don't renumbered §09, Assets §10; swatches list hex and RGB, each copyable on its own line (replaces the chip copy band); the cover gains two downloads: the full pack and a four-mark essentials zip (flat vector, 3D render, both globe masters); corrected §05's inverted depth claim (light's terracotta is the deeper value, not dark's); review pass: the §06 foil specimen now actually renders the foil (the cascade painted it ink), light-field Do/Don't status text derives an AA ink-mix, print pins the foil/badge/pill fills, the closed mobile drawer leaves the tab order, and scroll regions plus copy feedback are keyboard- and screen-reader-accessible.
v1.7.0  2026-07-16  — The hero mark's shadow now warms on paper instead of dropping black; added a skip link ahead of the section list; §02 zones captioned as fixed identity swatches; §03 notes the 3D render is one asset for both themes; print's pure-white field is documented as the one sanctioned exception to the never-#FFF rule.
v1.6.5  2026-07-16  — Do/Don't tiles and the on-ink/on-paper plates now pin the field they depict, so a specimen no longer borrows the page theme's palette (the terracotta foil took the daylight ramp on the ink card in light mode, and the paper tiles' Do/Don't badges were unreadable in dark mode); the light-theme top bar drops backdrop-blur for an opaque paper bar, per the kit's own light/dark law; printing now carries the full light token set instead of only background and ink, and specimen fields survive the printer's background stripping.
v1.6.4  2026-07-14  — Reverted the §02 "you are here" zone (v1.6.3): back to the original theme-driven outline (light in dark mode, dark in light mode).
v1.6.2  2026-07-14  — Click-to-copy swatch band now fills the chip's full top edge (removed the 1px inset gap).
v1.6.1  2026-07-14  — Hero mark now renders in 3D; equalized the §03 on-ink/on-paper plates so both marks align (facets moved to a row below); vertically centered the three-lockup tiles.
v1.6.0  2026-07-14  — Refreshed the 3D shield render with deeper relief (updated the 3D PNGs and the download pack) and added a §03 note that the finish only reads at large, premium sizes—ship the flat vector below that.
v1.5.0  2026-07-14  Navigation redesign across breakpoints (safe-area/notch fix, click-to-close scrim), sun/moon theme toggle icon, click-to-copy colour swatches.
v1.4.0  2026-07-14  — Light is now the default theme (dark stays the canonical reference); campaigns lead in light; replaced the mislabeled "app glyph" favicon lockup with an honest favicon entry plus an illustrative iOS app-icon mockup (squircle tile).
v1.3.0  2026-07-14  Renamed the primary mark from "circular emblem" to "shield" across §03/§04/§11 (matches the asset and the Predixa kit), added a §03 flat-vs-3D finish showcase, and renamed the download to TMX-ecosystem-assets.zip.
v1.2.1  2026-07-14  — Neutral kit-purpose framing (dropped co-branded assumption), removed the TMX Global disclaimer sentence, globe emblem now switches dark/light with theme.
v1.2.0  2026-07-12  Added WCAG contrast ratios to colour swatches and corrected the mislabeled deep-shade AA claim, in-kit contact/approval line, machine-readable token export (tokens.css/json).
v1.1.0  2026-07-12  Fixed self-referential §01 line, "colour shield"→"mark", dark-foil swatch roles, "two daylights"→"two settings", §09 status-fill contradiction, added §03 globe dark/light note, quieter umbrella voice, monotonic foil gradient (light to dark), copy tightening.
v1.0.0  2026-07-12  Initial TMX Ecosystem partner kit.
